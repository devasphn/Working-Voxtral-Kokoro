# PRODUCTION-READY Voxtral Model Requirements
# Core components: VAD + ASR + LLM
# Optimized for RunPod deployment

# Core PyTorch Stack (CUDA 12.1 compatible)
torch>=2.1.0,<2.5.0
torchaudio>=2.1.0,<2.5.0
torchvision>=0.16.0,<0.20.0

# Transformers with compatible huggingface-hub
transformers>=4.56.0
huggingface-hub>=0.34.0
accelerate>=0.25.0
tokenizers>=0.15.0

# Mistral Common with Audio Support (OFFICIAL) - v1.8.5 with AudioURLChunk & TranscriptionRequest
mistral-common[audio]==1.8.5

# Audio Processing
librosa>=0.10.1
soundfile>=0.12.1
numpy>=1.24.0,<2.0.0
scipy>=1.11.0

# Web Framework - Production versions
fastapi>=0.107.0
uvicorn[standard]>=0.24.0
websockets>=12.0
pydantic>=2.9.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6
aiofiles>=23.2.1
httpx>=0.25.0

# Audio processing and system utilities
pyaudio>=0.2.11
pillow>=10.1.0

# Configuration and utilities
pyyaml>=6.0.1
python-dotenv>=1.0.0
psutil>=5.9.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
wheel
# Flash Attention for Voxtral optimization (optional)
pip install flash-attn --no-build-isolation
